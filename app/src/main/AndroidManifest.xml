<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dam.app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_field"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_field_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FutbolParaTroncos"

        android:usesCleartextTraffic="true">

        <activity android:name="dam.app.activity.ActivityFields" android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
</manifest>